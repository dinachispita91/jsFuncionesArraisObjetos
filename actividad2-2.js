console.log(`A liarla con las funciones`);


function saludar (persona , apellido ) {  //los nombres de dentro del parentesis se llaman parametros
     console.log(`Hola  ${persona } ${ apellido}`)  //⚠️⚠️cuidado con el orden de los parametros
}

saludar(`clase ` , `Desarrollo web entorno cliente`)
